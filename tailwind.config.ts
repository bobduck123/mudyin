// Tailwind v4 — theme configuration lives in src/app/globals.css via @theme blocks.
// This file is kept minimal; content detection is automatic in v4.
const tailwindConfig = {}

export default tailwindConfig
